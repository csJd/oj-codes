#include<cstdio>
#include<cstring>
using namespace std;


bool hard(int l,char c)
{
    s[l]=c;
    for(int k=1;)
}

void dfs(cur)
{
    if(cnt==n)
    {
        for(int i=1;i<=cur;++i)
            printf("%c",s[i]);
        s[cur+1]='\0';
        printf("\n%d\n",strlen(s));
    }

    for(int i=0;i<l;++i)
    {
        if(!vis[i+'A']&&hard(cur,i))
    }
}

int main()
{
    while(scanf("%d%d",&n,&l),n)
    {
        memset(vis,0,sizeof(vis));

    }
}
