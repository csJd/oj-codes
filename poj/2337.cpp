#include<cstdio>
#include<cstring>
#include<map>
using namespace std;
int main()
{
    scanf("%d",&cas);
    while(cas--)
    {
        scanf("%d",&n);
        for(int i=1;i<=n;++i)
            scanf("%s",word[i]);
    }
}
