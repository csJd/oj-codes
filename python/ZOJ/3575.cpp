#include <bits/stdc++.h>
using namespace std;
const int N = 205;
const double eps = 1e-8;
double a, b, x0, y0;
double x[N], y[N];


int main()
{
	while(~scanf("%lf%lf", &a, &b))
	{
		scanf("%d", &n);
		for(int i = 0; i < n; ++i)
			scanf("%lf%lf", &x[i] ,&y[i]);

		if(!n || !a || !b)
		{
			puts("0");
			continue;
		}








	
    
    return 0;
}
//Last modified :   2015-08-02 16:13
