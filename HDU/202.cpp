#include<cstdio>
#include<cstring>
using namespace std;
int v[3005], ans[3005];
int main()
{
    int a, b, r, cnt;
    while(~scanf("%d%d", &a, &b))
    {
        memset(v, 0, sizeof(v));
        r = cnt = 0;
        while(!v[r])
        {
        }
        for(int i = 1; i <= cnt; ++i) printf("%d", ans[i]);
    }
    return 0;
}
